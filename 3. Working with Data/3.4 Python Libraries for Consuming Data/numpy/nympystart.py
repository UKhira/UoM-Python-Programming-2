# you must install numpy before work with it
# type "pip install numpy" in comamnd prompt to install

# numpy is a python library that used to work with arrays
import numpy as np
print(np.__version__)
# Check version